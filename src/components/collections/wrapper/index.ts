import Root from "./Root.vue";
import Block from "./Block.vue";
import Container from "./Container.vue";

export const wrapperMapper: Record<string, any> = {
    root: Root,
    block: Block,
    container: Container,
};
